# Copied automatically to web/.env.local by `pnpm db:seed` — edit if you want to point
# the dashboard at a different sandbox instance or a different merchant's API key.
VITE_API_BASE_URL=http://localhost:4000
VITE_DEMO_API_KEY=
